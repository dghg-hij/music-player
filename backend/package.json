{
  "name": "music-player-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute music-player-db --file=./schema.sql",
    "db:init:local": "wrangler d1 execute music-player-db --local --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.0.0",
    "@cloudflare/workers-types": "^4.0.0"
  }
}
